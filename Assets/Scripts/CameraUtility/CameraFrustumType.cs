namespace GameStudio.HunterGatherer.CameraUtility
{
    /// <summary>An enum to determine what type of frustum is being drawn</summary>
    public enum CameraFrustumType
    {
        Schematic,
        Realistic
    }
}