namespace GameStudio.HunterGatherer.Divisions
{
    /// <summary>Enum that defines if a hit was succesful, blocked or missed</summary>
    public enum HitType
    {
        Hit,
        Block,
        Miss
    }
}