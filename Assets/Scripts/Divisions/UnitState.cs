namespace GameStudio.HunterGatherer.Divisions
{
    /// <summary>Enum describing the current state of a unit</summary>
    public enum UnitState
    {
        Idle,
        Move,
        Attack,
        Death
    }
}